# Development environment variables
# Env vars should be placed in the repo root
# - nebula (root)
# -- nebula-installer

###
### nebula-installer
###

# Set to 1 to skip the network step in the installer
NEBULA_SKIP_NETWORK=0

# Set to 1 to force offline-only install behavior
NEBULA_OFFLINE_ONLY=0

# Force detected GPU vendors (comma separated), e.g. nvidia,intel,amd
NEBULA_DEV_GPU=

# Allow running installer without root (dev only)
NEBULA_DEV_ALLOW_NONROOT=0

# Adjust the terminal wrapper outer gap (used by live scripts)
NEBULA_OUTER_GAP=24

# Skip building the offline package repo during ISO builds
NEBULA_SKIP_OFFLINE_REPO=0

# Installer Base URL for pacman mirrors, e.g. https://mirror.nebulalinux.com/stable
NEBULA_PACMAN_MIRROR=https://mirror.nebulalinux.com/stable

# Installer Full mirrorlist contents (overrides NEBULA_PACMAN_MIRROR)
# Example:
# NEBULA_PACMAN_MIRRORLIST=Server = https://stable.mirror.nebulalinux.com/$repo/os/$arch\nServer = https://backup.mirror.nebulalinux.com/$repo/os/$arch
NEBULA_PACMAN_MIRRORLIST=

###
### nebula-iso
###

# Resolve Offline Deps mirror url. e.g. https://mirror.nebulalinux.com/stable or https://mirror.nebulalinux.com/unstable
RESOLVE_MIRROR_URL=https://mirror.nebulalinux.com/stable
